{
  "name": "voxon-firebase",
  "version": "0.1.0",
  "private": true,
  "description": "Reglas, índices y Cloud Functions de VOXON, con sus pruebas contra los emuladores",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-voxon \"node --test tests/*.test.mjs\"",
    "test:functions": "npm --prefix functions test && firebase emulators:exec --only auth,firestore,functions --project demo-voxon \"node --test functions/test/*.emulator.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.22.4"
  }
}
