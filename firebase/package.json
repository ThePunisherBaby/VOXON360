{
  "name": "voxon90-firebase",
  "version": "0.1.0",
  "private": true,
  "description": "Reglas de Firestore para la app del dueño de VOXON90 y sus pruebas con el emulador",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-voxon90 \"node --test tests/*.test.mjs\"",
    "test:sync": "firebase emulators:exec --only auth,firestore --project demo-voxon90 \"cd ../backend/server && VOXON_FIREBASE_EMULATOR=127.0.0.1 ./gradlew test --tests *CloudEmulatorTest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.22.4"
  }
}
